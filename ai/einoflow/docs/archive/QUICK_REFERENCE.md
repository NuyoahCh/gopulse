# ⚡ EinoFlow 快速参考

## 🚀 启动服务

```bash
# 方式 1: 使用启动脚本（推荐）
./scripts/start-dev.sh

# 方式 2: 手动启动
# 终端 1 - 后端
go run cmd/server/main.go

# 终端 2 - 前端
cd web
npm run dev
```

访问: `http://localhost:5173`

---

## ⏱️ 响应时间参考

| 功能 | 预期时间 | 备注 |
|------|---------|------|
| 获取模型列表 | < 1秒 | 快速 |
| 普通对话 | 2-5秒 | 正常 |
| 流式对话 | 实时 | 逐字显示 |
| RAG 索引 | 0.5-2秒 | 取决于文档数 |
| RAG 查询 | 10-20秒 | 检索+生成 |
| Agent 执行 | 1-2分钟 | 复杂推理 |
| **Graph 执行** | **3-4分钟** | ⏱️ 多步骤处理 |

---

## 📋 功能页面

| 页面 | URL | 说明 |
|------|-----|------|
| 首页 | `/` | 功能导航 |
| AI 对话 | `/chat` | 与 AI 聊天 |
| AI Agent | `/agent` | 执行复杂任务 |
| RAG 检索 | `/rag` | 文档索引和查询 |
| Graph 编排 | `/graph` | 多步骤处理 |

---

## 🔧 常见问题

### Chat 显示"连接失败"

**原因**: 后端未运行或格式不匹配

**解决**:
```bash
# 1. 检查后端
curl http://localhost:8080/api/v1/llm/models

# 2. 重启后端
go run cmd/server/main.go

# 3. 重启前端
cd web && npm run dev
```

---

### Graph 超时或卡死

**原因**: Graph 需要 3-4 分钟执行

**解决**: 
- ✅ 耐心等待 3-4 分钟
- ✅ 不要刷新页面
- ✅ 查看加载状态

**如果还是超时**:
```typescript
// web/src/api/client.ts
timeout: 600000  // 增加到 10 分钟
```

---

### Agent 超时

**原因**: Agent 需要 1-2 分钟

**解决**: 等待足够时间

---

### RAG 查询无结果

**原因**: 未索引文档

**解决**:
1. 切换到"索引文档"
2. 输入文档内容
3. 点击"开始索引"
4. 切换回"查询问答"

---

## 🧪 快速测试

### 测试 Chat
```
输入: 你好，请介绍一下 Go 语言
预期: 2-5秒内收到回复
```

### 测试 Agent
```
任务: 给我写一篇关于 Go 语言的文章
预期: 1-2分钟后显示文章
```

### 测试 RAG
```
1. 索引文档:
   Go 是一门编程语言
   Go 由 Google 开发
   
2. 查询:
   Go 是什么？
   
预期: 10-20秒后显示答案
```

### 测试 Graph
```
问题: 如何设计微服务架构？
预期: 3-4分钟后显示结果
⏱️ 需要耐心等待！
```

---

## 📊 API 端点

### LLM
- `GET /api/v1/llm/models` - 获取模型列表
- `POST /api/v1/llm/chat` - 普通对话
- `POST /api/v1/llm/chat/stream` - 流式对话

### Agent
- `POST /api/v1/agent/run` - 运行 Agent

### RAG
- `POST /api/v1/rag/index` - 索引文档
- `POST /api/v1/rag/query` - 查询问答
- `GET /api/v1/rag/stats` - 获取统计
- `POST /api/v1/rag/clear` - 清空文档

### Graph
- `POST /api/v1/graph/run` - 运行 Graph

---

## 🔍 调试技巧

### 查看后端日志
```bash
# 后端日志会显示在运行后端的终端
# 查找请求记录
[GIN] ... | 200 | ... | POST "/api/v1/..."
```

### 查看前端日志
```
F12 → Console → 查看错误
F12 → Network → 查看请求
```

### 测试 API
```bash
# 测试模型列表
curl http://localhost:8080/api/v1/llm/models

# 测试对话
curl -X POST http://localhost:8080/api/v1/llm/chat \
  -H "Content-Type: application/json" \
  -d '{
    "provider": "ark",
    "model": "doubao-seed-1-6-lite-251015",
    "messages": [{"role": "user", "content": "你好"}]
  }'
```

---

## 📝 配置文件

### 环境变量 (`.env`)
```bash
# ARK (豆包)
ARK_API_KEY=your_ark_api_key
ARK_BASE_URL=https://ark.cn-beijing.volces.com/api/v3

# OpenAI
OPENAI_API_KEY=your_openai_key
OPENAI_BASE_URL=https://api.openai.com/v1

# Embedding
ARK_EMBEDDING_MODEL=doubao-embedding-large-text-250515
```

### Vite 配置 (`web/vite.config.ts`)
```typescript
proxy: {
  '/api': {
    target: 'http://localhost:8080',
    changeOrigin: true,
    secure: false,
  }
}
```

### API 超时 (`web/src/api/client.ts`)
```typescript
timeout: 300000  // 5 分钟
```

---

## 🎯 性能优化建议

### 立即可做
1. 使用流式输出（Chat 已支持）
2. 索引更多文档提高 RAG 准确性
3. 使用更具体的问题描述

### 需要开发
1. Graph 流式输出
2. 结果缓存
3. 并行执行优化

---

## 📚 文档索引

- `README.md` - 项目介绍
- `FIXES_ROUND3.md` - 最新修复
- `DEBUG_GUIDE.md` - 调试指南
- `TEST_CHECKLIST.md` - 测试清单
- `web/SETUP.md` - 前端设置

---

## ⚠️ 重要提示

1. **Graph 执行需要 3-4 分钟** ⏱️
   - 这是正常的
   - 请耐心等待
   - 不要刷新页面

2. **Agent 执行需要 1-2 分钟**
   - 复杂任务需要时间
   - 页面会显示加载状态

3. **RAG 查询需要 10-20 秒**
   - 需要检索和生成
   - 等待时间正常

4. **所有功能都需要后端运行**
   - 确保后端在 8080 端口
   - 确保前端在 5173 端口

---

## ✅ 快速检查清单

启动前检查：
- [ ] `.env` 文件已配置
- [ ] 后端运行在 8080 端口
- [ ] 前端运行在 5173 端口

功能检查：
- [ ] Chat 可以加载模型列表
- [ ] Chat 可以发送消息
- [ ] Agent 可以执行任务（等待 1-2 分钟）
- [ ] RAG 可以索引文档
- [ ] RAG 可以查询问答（等待 10-20 秒）
- [ ] Graph 可以执行（等待 3-4 分钟）

---

## 🎉 享受使用 EinoFlow！

如有问题，查看详细文档或检查后端日志。

# 🚀 新功能快速开始

欢迎使用 EinoFlow v1.1.0！本指南帮助你快速体验三个新功能。

---

## 📋 前置准备

### 1. 启动服务

```bash
# 确保已配置 .env 文件
cp .env.example .env
# 编辑 .env，填入你的 API Key

# 启动后端
go run cmd/server/main.go

# 启动前端（新终端）
cd web && npm run dev
```

### 2. 验证服务

```bash
# 检查后端
curl http://localhost:8080/health

# 访问前端
open http://localhost:5173
```

---

## 🌤️ 功能 1：天气查询（30 秒体验）

### 方式 A：前端体验

1. 打开浏览器：`http://localhost:5173`
2. 点击 **Agent** 页面
3. 输入：`北京今天天气怎么样？`
4. 点击 **执行任务**
5. 等待 5-10 秒，查看实时天气信息

### 方式 B：API 调用

```bash
curl -X POST http://localhost:8080/api/v1/agent/run \
  -H "Content-Type: application/json" \
  -d '{"task": "上海的温度是多少？"}'
```

### 💡 试试这些问题

- "深圳会下雨吗？"
- "杭州现在气温多少度？"
- "成都的天气怎么样？"

---

## 📁 功能 2：RAG 文件上传（1 分钟体验）

### 步骤 1：准备测试文件

```bash
cat > my_document.txt << 'EOF'
EinoFlow 使用指南

快速开始：
1. 配置环境变量
2. 启动后端服务
3. 启动前端界面
4. 开始使用

主要功能：
- LLM 对话
- Agent 系统
- RAG 检索
- Chain 编排
EOF
```

### 步骤 2：上传文件

**前端方式：**
1. 访问 **RAG** 页面
2. 点击 **索引文档** 标签
3. 滚动到"上传文档文件"区域
4. 选择 `my_document.txt`
5. 点击 **上传并索引**

**API 方式：**
```bash
curl -X POST http://localhost:8080/api/v1/rag/upload \
  -F "file=@my_document.txt"
```

### 步骤 3：查询测试

1. 切换到 **查询问答** 标签
2. 输入：`EinoFlow 有哪些主要功能？`
3. 点击 **查询**
4. 查看 AI 回答和参考来源

---

## 🎨 功能 3：Markdown 渲染（即时体验）

### 测试方法

在 **Chat** 页面输入以下提示词：

```
请用 Markdown 格式介绍 Python，包括：
1. 标题和副标题
2. 代码示例（带语法高亮）
3. 特性列表
4. 对比表格
```

### 查看效果

你会看到：
- ✅ 清晰的标题层级
- ✅ 语法高亮的代码块
- ✅ 格式化的列表
- ✅ 美观的表格
- ✅ 可点击的链接

---

## 🎯 完整工作流演示

### 场景：创建一个技术文档助手

#### 1. 上传技术文档

```bash
cat > tech_doc.txt << 'EOF'
# Go 语言快速排序

## 实现代码

func quickSort(arr []int) []int {
    if len(arr) < 2 {
        return arr
    }
    pivot := arr[0]
    var less, greater []int
    for _, v := range arr[1:] {
        if v <= pivot {
            less = append(less, v)
        } else {
            greater = append(greater, v)
        }
    }
    return append(append(quickSort(less), pivot), quickSort(greater)...)
}

## 时间复杂度
- 最好情况：O(n log n)
- 平均情况：O(n log n)
- 最坏情况：O(n²)

## 使用场景
适用于大规模数据排序，性能优秀。
EOF

curl -X POST http://localhost:8080/api/v1/rag/upload \
  -F "file=@tech_doc.txt"
```

#### 2. 查询技术问题

```bash
curl -X POST http://localhost:8080/api/v1/rag/query \
  -H "Content-Type: application/json" \
  -d '{
    "query": "请用 Markdown 格式解释快速排序的实现和复杂度"
  }'
```

#### 3. 查询天气（可选）

```bash
curl -X POST http://localhost:8080/api/v1/agent/run \
  -H "Content-Type: application/json" \
  -d '{
    "task": "北京今天适合户外编程吗？查一下天气"
  }'
```

---

## 📊 功能对比

| 功能 | 使用场景 | 响应时间 | 特点 |
|------|---------|---------|------|
| 天气查询 | 实时信息获取 | 5-10秒 | 支持主要城市 |
| 文件上传 | 批量文档索引 | 10-30秒 | 支持 TXT/MD |
| Markdown 渲染 | 内容展示 | 即时 | 完整格式支持 |

---

## 🎓 学习路径

### 初学者
1. ✅ 先体验天气查询（最简单）
2. ✅ 尝试 Markdown 渲染（即时反馈）
3. ✅ 最后测试文件上传（需要准备文件）

### 进阶用户
1. 组合使用多个功能
2. 上传自己的文档
3. 自定义 Markdown 样式
4. 扩展更多工具

### 开发者
1. 查看源代码实现
2. 添加新的工具
3. 优化 Markdown 渲染
4. 贡献代码

---

## 💡 使用技巧

### 天气查询
- 使用完整城市名（"北京"而不是"BJ"）
- 可以问具体问题（"温度"、"会下雨吗"）
- 支持自然语言（"今天"、"现在"）

### 文件上传
- 优先使用 UTF-8 编码
- 文件大小 < 10MB
- TXT 和 MD 格式最佳
- 可以上传代码文件

### Markdown 渲染
- 要求 AI 使用 Markdown 格式
- 代码块指定语言获得高亮
- 表格自动格式化
- 链接自动在新标签打开

---

## 🐛 常见问题

### Q1: 天气查询失败？
**A:** 检查网络连接，确认城市名正确，尝试其他城市。

### Q2: 文件上传后查询没结果？
**A:** 确认文件已成功上传，使用更具体的查询关键词。

### Q3: Markdown 不显示？
**A:** 刷新页面，检查浏览器控制台错误，确认前端服务正常。

### Q4: 如何查看更多文档？
**A:** 查看 `docs/` 目录：
- `NEW_FEATURES.md` - 详细功能说明
- `TESTING_GUIDE.md` - 完整测试指南
- `SECURITY.md` - 安全配置指南

---

## 🎉 下一步

### 探索更多
- 📖 阅读 [完整文档](docs/NEW_FEATURES.md)
- 🧪 运行 [测试指南](docs/TESTING_GUIDE.md)
- 🔧 查看 [配置指南](docs/CONFIG_GUIDE.md)
- 🔒 了解 [安全最佳实践](docs/SECURITY.md)

### 贡献代码
- Fork 项目
- 添加新功能
- 提交 Pull Request
- 参与讨论

### 获取帮助
- 提交 Issue
- 查看文档
- 加入社区

---

## 📞 反馈

如果你喜欢这些新功能，请：
- ⭐ Star 项目
- 📝 提供反馈
- 🐛 报告 Bug
- 💡 建议新功能

感谢使用 EinoFlow！🚀

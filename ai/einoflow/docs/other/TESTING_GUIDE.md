# 新功能测试指南

## 🧪 测试概览

本指南帮助你快速测试三个新功能：
1. 天气查询工具
2. RAG 文件上传
3. Markdown 渲染

---

## 🌤️ 测试一：天气查询功能

### 前置条件
- ✅ 后端服务已启动
- ✅ 配置了有效的 LLM API Key（ARK 或 OpenAI）
- ✅ 网络连接正常（需要访问 wttr.in）

### 测试步骤

#### 1. API 测试

```bash
# 测试北京天气
curl -X POST http://localhost:8080/api/v1/agent/run \
  -H "Content-Type: application/json" \
  -d '{
    "task": "北京今天天气怎么样？"
  }'

# 测试上海天气
curl -X POST http://localhost:8080/api/v1/agent/run \
  -H "Content-Type: application/json" \
  -d '{
    "task": "上海的温度是多少？"
  }'

# 测试深圳天气
curl -X POST http://localhost:8080/api/v1/agent/run \
  -H "Content-Type: application/json" \
  -d '{
    "task": "深圳会下雨吗？"
  }'
```

#### 2. 前端测试

1. 打开浏览器访问：`http://localhost:5173`
2. 点击导航栏的 "Agent" 页面
3. 在任务输入框中输入以下问题之一：
   - "北京今天天气怎么样？"
   - "查询上海的天气"
   - "杭州现在气温多少度？"
4. 点击"执行任务"按钮
5. 等待 5-10 秒，查看返回结果

### 预期结果

✅ **成功响应示例：**
```
北京当前天气：晴，温度5°C，体感温度2°C，湿度45%，风速15 km/h
```

❌ **失败响应示例：**
```
抱歉，暂时无法获取北京的实时天气信息。建议您：
1. 访问中国气象局官网：http://www.weather.com.cn
2. 使用手机天气APP查询
3. 搜索引擎搜索"北京今日天气"
```

### 常见问题

**Q: 为什么查询失败？**
- 检查网络连接
- 确认 wttr.in 服务是否可访问
- 尝试使用其他城市名

**Q: 支持哪些城市？**
- 主要支持中国一二线城市
- 完整列表见 `internal/agent/react.go` 中的 `cities` 数组

---

## 📁 测试二：RAG 文件上传功能

### 前置条件
- ✅ 后端服务已启动
- ✅ 配置了 Embedding 模型（ARK_EMBEDDING_MODEL）
- ✅ 准备测试文件（TXT 或 MD 格式）

### 准备测试文件

创建一个测试文件 `test_document.txt`：

```bash
cat > test_document.txt << 'EOF'
EinoFlow 是一个基于 Eino 框架的 AI 应用平台。

主要功能：
1. LLM 对话：支持多种模型提供商
2. Agent 系统：ReAct Agent 支持工具调用
3. RAG 检索：文档索引和智能问答
4. Chain 编排：多步骤任务处理
5. Graph 工作流：复杂任务编排

技术栈：
- 后端：Go + Gin + Eino
- 前端：React + TypeScript + Vite
- 数据库：SQLite
- 向量存储：内存/持久化

特点：
- 开箱即用
- 易于扩展
- 完整文档
- 生产就绪
EOF
```

### 测试步骤

#### 1. API 测试

```bash
# 上传文件
curl -X POST http://localhost:8080/api/v1/rag/upload \
  -F "file=@test_document.txt"

# 查询文档
curl -X POST http://localhost:8080/api/v1/rag/query \
  -H "Content-Type: application/json" \
  -d '{
    "query": "EinoFlow 有哪些主要功能？"
  }'

# 查看统计
curl http://localhost:8080/api/v1/rag/stats
```

#### 2. 前端测试

**步骤 1：上传文件**
1. 访问 `http://localhost:5173`
2. 点击 "RAG" 页面
3. 确保在"索引文档"标签页
4. 滚动到"上传文档文件"区域
5. 点击"选择文件"，选择 `test_document.txt`
6. 点击"上传并索引"按钮
7. 等待上传完成，查看成功消息

**步骤 2：查询测试**
1. 切换到"查询问答"标签
2. 输入问题："EinoFlow 有哪些主要功能？"
3. 点击"查询"按钮
4. 查看 AI 回答和参考来源

### 预期结果

✅ **上传成功响应：**
```json
{
  "message": "File uploaded and indexed successfully",
  "filename": "test_document.txt",
  "document_count": 8,
  "total_count": 8
}
```

✅ **查询成功响应：**
```json
{
  "answer": "EinoFlow 的主要功能包括：\n1. LLM 对话：支持多种模型提供商\n2. Agent 系统：ReAct Agent 支持工具调用\n3. RAG 检索：文档索引和智能问答\n4. Chain 编排：多步骤任务处理\n5. Graph 工作流：复杂任务编排",
  "documents": [
    "EinoFlow 是一个基于 Eino 框架的 AI 应用平台。\n\n主要功能：...",
    "技术栈：\n- 后端：Go + Gin + Eino..."
  ]
}
```

### 测试不同文件类型

```bash
# 测试 Markdown 文件
cat > test.md << 'EOF'
# EinoFlow 使用指南

## 快速开始

1. 安装依赖
2. 配置环境变量
3. 启动服务

## 功能特性

- **LLM 对话**：多模型支持
- **RAG 检索**：智能问答
- **Agent 系统**：工具调用
EOF

curl -X POST http://localhost:8080/api/v1/rag/upload \
  -F "file=@test.md"
```

### 常见问题

**Q: 上传失败怎么办？**
- 检查文件大小（< 10MB）
- 确认文件格式（TXT, MD）
- 查看后端日志

**Q: 查询没有结果？**
- 确认文件已成功上传
- 检查查询关键词是否相关
- 尝试更具体的问题

---

## 🎨 测试三：Markdown 渲染功能

### 前置条件
- ✅ 前端服务已启动
- ✅ 浏览器支持现代 CSS

### 测试步骤

#### 1. 测试基础 Markdown

在 Chat 页面输入以下提示词：

```
请用 Markdown 格式介绍 Python 编程语言，包括：
1. 标题和副标题
2. 代码示例
3. 列表
4. 表格
```

#### 2. 测试代码高亮

```
请用 Markdown 格式展示以下代码：

Python 函数示例：
```python
def fibonacci(n):
    if n <= 1:
        return n
    return fibonacci(n-1) + fibonacci(n-2)
```

JavaScript 示例：
```javascript
const greet = (name) => {
    console.log(`Hello, ${name}!`);
};
```
```

#### 3. 测试表格

```
请用 Markdown 表格对比 Go 和 Python 的特点
```

#### 4. 测试列表和引用

```
请用 Markdown 格式列出学习编程的建议，并用引用块强调重点
```

### 预期效果

✅ **标题**：不同大小的标题样式
✅ **代码块**：深色背景，语法高亮，语言标签
✅ **行内代码**：灰色背景，圆角边框
✅ **列表**：清晰的项目符号和缩进
✅ **表格**：边框分隔，表头高亮
✅ **引用**：左侧蓝色边框，斜体文字
✅ **链接**：蓝色文字，悬停下划线

### 视觉检查清单

- [ ] 标题层级清晰（H1 > H2 > H3）
- [ ] 代码块有语法高亮
- [ ] 代码块右上角显示语言标签
- [ ] 表格可以横向滚动（如果内容过长）
- [ ] 链接在新标签页打开
- [ ] 列表项对齐整齐
- [ ] 引用块有明显视觉区分
- [ ] 整体排版美观易读

### 测试不同页面

1. **Chat 页面**：测试对话中的 Markdown
2. **Agent 页面**：测试 Agent 回复的 Markdown
3. **RAG 页面**：测试查询结果的 Markdown

---

## 🔄 集成测试

### 场景 1：天气查询 + Markdown 渲染

```bash
# 在 Agent 页面输入
"请查询北京天气，并用 Markdown 格式整理成表格"
```

**预期结果：**
- 获取实时天气数据
- 以 Markdown 表格形式展示
- 前端正确渲染表格

### 场景 2：文件上传 + RAG 查询 + Markdown

```bash
# 1. 上传包含代码的文档
cat > code_doc.txt << 'EOF'
Python 快速排序实现：

def quick_sort(arr):
    if len(arr) <= 1:
        return arr
    pivot = arr[len(arr) // 2]
    left = [x for x in arr if x < pivot]
    middle = [x for x in arr if x == pivot]
    right = [x for x in arr if x > pivot]
    return quick_sort(left) + middle + quick_sort(right)

使用示例：
numbers = [3, 6, 8, 10, 1, 2, 1]
sorted_numbers = quick_sort(numbers)
print(sorted_numbers)
EOF

# 2. 上传文件
curl -X POST http://localhost:8080/api/v1/rag/upload \
  -F "file=@code_doc.txt"

# 3. 查询并要求 Markdown 格式
curl -X POST http://localhost:8080/api/v1/rag/query \
  -H "Content-Type: application/json" \
  -d '{
    "query": "请用 Markdown 格式展示快速排序的代码和说明"
  }'
```

**预期结果：**
- 文件成功上传并索引
- 查询返回相关代码
- 代码以 Markdown 格式展示
- 前端正确高亮 Python 代码

### 场景 3：完整工作流

1. **上传文档**：上传项目文档
2. **天气查询**：询问当前天气
3. **RAG 查询**：基于文档提问
4. **Markdown 展示**：所有回复都格式化展示

---

## 📊 性能测试

### 天气查询性能

```bash
# 测试响应时间
time curl -X POST http://localhost:8080/api/v1/agent/run \
  -H "Content-Type: application/json" \
  -d '{"task": "北京天气"}'
```

**预期：** < 10 秒

### 文件上传性能

```bash
# 生成大文件测试
dd if=/dev/urandom of=large_file.txt bs=1M count=5

# 测试上传时间
time curl -X POST http://localhost:8080/api/v1/rag/upload \
  -F "file=@large_file.txt"
```

**预期：** < 30 秒（5MB 文件）

### Markdown 渲染性能

在浏览器开发者工具中：
1. 打开 Performance 面板
2. 开始录制
3. 发送包含大量 Markdown 的请求
4. 停止录制并分析

**预期：** 渲染时间 < 100ms

---

## 🐛 问题排查

### 天气查询失败

```bash
# 检查网络连接
curl https://wttr.in/Beijing?format=j1

# 检查后端日志
tail -f logs/einoflow.log | grep weather
```

### 文件上传失败

```bash
# 检查文件权限
ls -la test_document.txt

# 检查后端日志
tail -f logs/einoflow.log | grep upload

# 检查存储空间
df -h
```

### Markdown 不渲染

1. 打开浏览器控制台（F12）
2. 查看 Console 错误
3. 检查 Network 请求
4. 确认 CSS 加载正常

---

## ✅ 测试检查清单

### 天气查询
- [ ] API 调用成功
- [ ] 前端显示正常
- [ ] 支持多个城市
- [ ] 错误处理正确
- [ ] 响应时间合理

### 文件上传
- [ ] TXT 文件上传成功
- [ ] MD 文件上传成功
- [ ] 文件大小限制有效
- [ ] 分块索引正确
- [ ] 查询结果准确

### Markdown 渲染
- [ ] 标题渲染正确
- [ ] 代码高亮工作
- [ ] 表格显示正常
- [ ] 列表格式正确
- [ ] 链接可点击
- [ ] 引用块显示
- [ ] 响应式布局

---

## 📝 测试报告模板

```markdown
# 测试报告

## 测试信息
- 测试日期：2025-11-18
- 测试人员：[你的名字]
- 测试环境：[开发/测试/生产]

## 测试结果

### 天气查询
- [ ] 通过
- [ ] 失败
- 备注：

### 文件上传
- [ ] 通过
- [ ] 失败
- 备注：

### Markdown 渲染
- [ ] 通过
- [ ] 失败
- 备注：

## 发现的问题
1. 
2. 
3. 

## 改进建议
1. 
2. 
3. 
```

---

## 🚀 自动化测试（未来）

计划添加的自动化测试：

```go
// internal/tools/weather_test.go
func TestWeatherTool_GetWeather(t *testing.T) {
    tool := NewWeatherTool()
    result, err := tool.GetWeather(context.Background(), "北京")
    assert.NoError(t, err)
    assert.Contains(t, result, "temperature")
}
```

```go
// internal/api/rag_handler_test.go
func TestRAGHandler_UploadFile(t *testing.T) {
    // 测试文件上传
}
```

```tsx
// web/src/components/MarkdownRenderer.test.tsx
describe('MarkdownRenderer', () => {
  it('renders code blocks with syntax highlighting', () => {
    // 测试代码高亮
  });
});
```

---

## 📞 需要帮助？

如果测试过程中遇到问题：
1. 查看 `docs/TROUBLESHOOTING.md`
2. 检查 `docs/NEW_FEATURES.md`
3. 提交 Issue 并附上测试日志
